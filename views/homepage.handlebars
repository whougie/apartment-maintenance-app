


{{#each users as |user| }}
<div class="row align-center mb-5">
    <div class="col-md-6">
        <h2>Welcome! {{user.name}}</h2>
    </div>
    <div class="col-md-6">
        <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
    </div>
</div>
<div>
    <div class="form-group">
        <button class="btn btn-seccondary" type="issue"> maintenance help request</button>
    </div>
    <div>
        <h4>Active issues</h4>
        <div id="active-issues"></div>
    </div>
    <div>
        <h4>Completed issues</h4>
        <div id="completed-issues"></div>
    </div>
<script src="../public/js/logic.js"></script>
</div>
{{/each}}




{{!--
home page:

User:
partial load of a page


-see the issues that have been created on a list/table

-see the data of issue (date to fix, status, handyman assigned, issue description)

-User

Ex. Only the user issues

UserId: whougie Issue: fix sink, apt 12, status: Not fixed

UserId: whougie Issue: fix sink, apt 12, status: Fixed

-Manager: 17check (need route to handle logic for manager)

-see all user issues on list/table

-create button assigned the handyman

-see the data of issue (date to fix, status, handyman assigned, issue description)

Ex. list all users issues

userId: whougie …

userId: Kee …

Issue page:

-Displays a form that contains:

--issue description

– Create a submit button






--}}